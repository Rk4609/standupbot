/**
 * Every page spans the whole screen; only the gutters grow on a wide one.
 * It used to sit in a centred 72rem column, which left a laptop or a
 * monitor mostly empty. The top bar uses the same gutters so its edges line
 * up with the page under it. A page that reads better narrow — a letter, a
 * payslip — constrains its own content.
 */
export const GUTTER = 'px-4 md:px-6 xl:px-10'
