/**
 * Label shown under AI-generated reports.
 *
 * Keep this in step with GROQ_MODEL on the backend (see services/groqService.js).
 */
export const AI_MODEL_LABEL = 'Powered by Groq · GPT-OSS 120B'
